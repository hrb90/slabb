import React from 'react';
import LogoutButton from './auth/logout_button';

const Main = props => (
  <div className="main-page">
    <h1>Sla🅱️️🅱️️</h1>
    <LogoutButton />
  </div>
);

export default Main;
